en:
  helpers:
    submit:
      create: Create %{model}
      submit: Save %{model}
      update: Update %{model}

  activerecord:
    models:
      book: Book

    attributes:
      book:
        author: Author
        memo: Memo
        picture: Picture
        title: Title
